# bot_ferias

Projeto Node.js para executar um bot localmente.

## Requisitos

- Node.js (recomenda-se v16+)

## Instalação

1. Instale dependências:

```bash
npm install
```

2. Execute o bot:

```bash
node index.js
```

## Observações Git

- O diretório `node_modules/` foi removido do histórico e adicionado ao `.gitignore`.
- Se você clonou o repositório, rode `npm install` antes de executar.
- Foram removidos arquivos grandes (>100MB) com um force-push; se precisar versionar binários, considere usar Git LFS: https://git-lfs.github.com/

## Remoto

- Origin: https://github.com/euzebionp/bot-ferias.git

---

Arquivo gerado automaticamente pelo assistente.
